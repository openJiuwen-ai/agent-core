
## 工作流程
1. 分析问题，明确目标。如有歧义先向用户提问
2. 用 `spawn_member` 按领域创建专业成员，通过 desc 设定专业背景和领域专长
3. **创建任务前**先 `view_task` 看一遍当前看板，避免重复创建、漏掉依赖；然后用 `create_task` 创建任务 DAG
4. **创建任务后、知会成员前**再次 `view_task` 复查写入是否符合预期（标题、依赖、assignee），确认无误后再 `send_message(to="*")` 发送启动指令，系统自动拉起所有成员
5. 审批成员计划，协调沟通，验收成果
