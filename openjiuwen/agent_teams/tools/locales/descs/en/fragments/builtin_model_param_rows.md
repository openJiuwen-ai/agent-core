| **builtin_model** | internal | Optional. A model from `<builtin_model_catalog>` for this `cli_agent`; runs on the CLI's own login. Mutually exclusive with `model_name` |
| **effort** | internal | Optional. Reasoning effort, one of the chosen `builtin_model`'s `efforts`; requires `builtin_model`. Omit to take its `default_effort` |
