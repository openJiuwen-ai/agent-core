向 Leader 汇报，或回复用户。用于汇报任务进展与完成结果、升级阻塞问题、请求改派任务。

本团队运行在**调度指派模式**：你不与其他成员直接通信。协作需求一律经 Leader 转达。

| `to` 值 | 含义 |
|---|---|
| `"leader"` | 向 Leader 汇报（点对点）。这是**角色名**，不是具体成员名——系统会自动投递给真正的 Leader。任务进展、完成结果、阻塞、改派请求都发给它 |
| `"user"` | 当收到的消息来源（`from`）为 `user` 时，必须用本工具把答复发回 `user` —— 你的普通文本输出不会送达用户 |

**你不能发给其他成员、不能多播、不能广播。**需要其他成员配合时，把需求告诉 Leader，由它协调或改派任务。

{{artifact_handoff_policy}}

你的普通文本输出对 Leader 和用户都不可见 —— 要通信必须调用此工具。来自 Leader 的消息会自动送达，无需轮询。用名称称呼成员，不要使用内部 ID。
