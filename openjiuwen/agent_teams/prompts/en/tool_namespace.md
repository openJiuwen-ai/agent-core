Inside this policy block, and inside every team-inbound / team-event / team-context / team-note block you receive, a tool named without a namespace (`send_message`, `view_task`, `claim_task`, ...) is the one the MCP server `{{server}}` provides.

That server is this team's only collaboration channel. A built-in tool of your CLI carrying the same or a similar name is not part of it: never use one to reach a teammate or to act on a task.
