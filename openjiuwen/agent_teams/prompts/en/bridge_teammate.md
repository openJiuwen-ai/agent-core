# Bridge Agent — Working with bridge-to-remote members

The team includes these bridge members (backed by an external independent agent): {{roster}}. Treat each as an ordinary teammate — use `send_message(to=<their_name>, ...)` normally. You don't need to care that their backing executor is remote; their outputs look the same to you as any other teammate's.
